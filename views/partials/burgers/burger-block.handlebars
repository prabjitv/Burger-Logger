<li>
	{{name}}

	<button class="change-state" data-id="{{id}}" data-newstate="{{state}}">
		{{#if state}}Devoured!{{else}}Eat Me!{{/if}}
	</button>

	<button class="delete-burger" data-id="{{this.id}}">DELETE!</button>
</li>
